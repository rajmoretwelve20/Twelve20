# Twelve20 Website

Static, text-first website for Twelve 20 Inc.

Quick deploy commands:

```bash
npm run deploy.web
# or (if you prefer)
./deploy.web
# On Windows PowerShell:
./deploy.web.ps1
```

Make sure `firebase` CLI is installed and you are logged in (`firebase login`).
